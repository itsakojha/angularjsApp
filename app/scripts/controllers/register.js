'use strict';

/**
 * @ngdoc function
 * @name eshopApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the eshopApp
 */
angular.module('eshopApp')
  .controller('RegisterCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
